# Core ML — PyTorch stack
# mamba-ssm installed separately in Dockerfile with --no-build-isolation (needs torch already present)
torch>=2.2.0

# Data
numpy>=1.26
xarray>=2024.1
zarr>=2.16
scipy>=1.12

# Geospatial (static feature computation + UrbClim reprojection)
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
rasterio>=1.3        # Path A: bilinear reprojection (regular EPSG:3035 grids)

# Metrics
scikit-image>=0.22   # SSIM

# Utilities
tqdm>=4.66
pyyaml>=6.0
pandas>=2.1

# ERA5 download
cdsapi>=0.6
cfgrib>=0.9
eccodes>=1.6
